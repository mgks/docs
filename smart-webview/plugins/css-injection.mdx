---
title: 'CSS Injection'
description: 'Dynamically injecting custom CSS styles.'
icon: 'css3'
---

import { Tabs, Tab } from 'mintlify/components';

This plugin allows the native application to dynamically inject custom CSS stylesheets or rules into the loaded web pages. (Planned for v7.4)

<Tabs>
 <Tab title="Android">
    <Info>
    Detailed documentation for the Android CSS Injection Plugin, focusing on using `WebView.evaluateJavascript()` to manipulate the DOM (e.g., adding `<style>` tags or modifying element styles directly), methods for triggering injection (e.g., based on URL, native events), and potential use cases, will be added here soon.
    </Info>
 </Tab>
 <Tab title="iOS">
    <Info>
    Detailed documentation for the iOS CSS Injection Plugin, focusing on using `WKWebView.evaluateJavaScript()` or `WKUserScript` to inject CSS rules or modify the DOM, methods for triggering injection, and potential use cases, will be added here soon.
    </Info>
 </Tab>
</Tabs>